# SpaceXAI (xAI) — server-side only. Never commit the real key.
# If XAI_API_KEY is empty, SynapseNest uses the Grok CLI token in ~/.grok/auth.json
# when that session includes api:access.
XAI_API_KEY=
XAI_MODEL=grok-4.6
